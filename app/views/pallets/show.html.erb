<% @pallet.colors.split(" ").each do |color_hex| %>
<% end %>

<%= render :partial => 'swiper', locals: {pallet: @pallet} %>

<div class="container">

  <p id="notice"><%= notice %></p>

  <p>
    <strong>Title:</strong>
    <%= @pallet.title %>
  </p>

  <p>
    <strong>Image:</strong>
    <%= @pallet.image_url %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= @pallet.description %>
  </p>

  <p>
    <strong>Colors count:</strong>
    <%= @pallet.colors_count %>
  </p>

  <p>
    <strong>Colors:</strong>
    <%= @pallet.colors %>
  </p>


  <% @pallet.colors.split(" ").each do |color_hex| %>
    <div class="row" style="margin-top: 50px; background-color: white;">
      <div class="ink-well col-md-3">
        <%= image_tag("inkBottle.svg", :style => "width: 100%; background-color: #{color_hex}") %>
      </div>
      <div class="col-md-offset-1 col-md-8">
        <h1 style="padding: 30px 0px;"><%= color_hex %> 白色</h1>
        <%= image_tag("handWriting.svg", :style => "width: 100%; background-color: #{color_hex}") %>
      </div>
    </div>
  <% end %>


</div>

<%= link_to 'Edit', edit_pallet_path(@pallet) %> |
<%= link_to 'Back', pallets_path %>
<input class="ink-water" type="color" id="colorChoice" >

<script>

</script>
